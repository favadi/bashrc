# Use vim style editing
set editing-mode vi

# Some key bindings
$if mode=vi
    set keymap vi-command
    "gg": beginning-of-history
    "G": end-of-history
    set keymap vi-insert
    "jk": vi-movement-mode
    "\C-l": clear-screen
$endif
